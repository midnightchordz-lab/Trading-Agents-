# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# IDE and editors
.idea/
.vscode/

# Dependencies
node_modules/
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Testing
/coverage

# Next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# Production builds
/build
dist/
dist

# Environment files (comprehensive coverage)

*token.json*
*credentials.json*

# Logs and debug files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
dump.rdb

# System files
.DS_Store
*.pem

# Python
__pycache__/
*pyc*
venv/
.venv/

# Development tools
chainlit.md
.chainlit
.ipynb_checkpoints/
.ac

# Deployment
.vercel

# Data and databases
agenthub/agents/youtube/db

# Archive files and large assets
**/*.zip
**/*.tar.gz
**/*.tar
**/*.tgz
*.pack
*.deb
*.dylib

# Build caches
.cache/

memory/test_credentials.md

# Mobile development
android-sdk/
credentials.json
*.key
.credentials
# NOTE: do NOT ignore .env / .env.* / *.env here. The Emergent deploy build
# context is built from the repo, so ignoring them ships a container with a
# STALE environment (this has already caused live Razorpay keys to be missing
# in production three times: payments then fail with "Authentication failed"
# and the app shows "Couldn't start checkout · HTTP 502"). Verify with
# `git check-ignore -v backend/.env` — it must print nothing.
# The three patterns below were appended a FOURTH time (2026-06-22) directly
# under this note; `tests/test_pay_health.py::test_env_files_are_not_git_ignored`
# is what caught it. They stay commented out, not deleted, so the next tool
# that regenerates this file has something to append to.
# .env
# .env.*
# *.env
.env
.env.*
*.env
